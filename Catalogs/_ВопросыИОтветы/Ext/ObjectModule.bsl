
Процедура ПередЗаписью(Отказ)

    Если не Ссылка.Пустая() и Родитель<>Ссылка.Родитель тогда
        
        Порядок =  СокрЛП(Родитель.Порядок) + ?(СокрЛП(Родитель.Порядок) = "","",".")
                + прав(СокрЛП(Порядок),СтрДлина(СокрЛП(Порядок)) - (СтрДлина(СокрЛП(Ссылка.Родитель.Порядок))+?(СокрЛП(Ссылка.Родитель.Порядок) = "",0,1))); 
        
    КонецЕсли;
    
КонецПроцедуры
